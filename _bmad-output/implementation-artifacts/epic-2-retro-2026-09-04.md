---
epic: 2
date: 2026-09-04
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Retrospective — Epic 2: Litigation Intelligence Tracker (Apex)

## Epic summary

- **Epic:** 2 — Litigation Intelligence Tracker (Apex) — 10 stories, all `done` (`sprint_status.py detect-epic --epic 2`: `pending_stories: []`)
- **Diff range:** `0b8a748^..43722af` — 10 non-merge commits (one squash PR per story), 0 merges, ~9,900 net added lines across 130+ files
- **Evidence inventory:**
  - ✅ Epic spec (`epics.md` L374–540, per-story acceptance criteria **declared**)
  - ✅ All 10 story files with review findings, dev records, and open questions
  - ✅ Full diff + `git_evidence.py` attribution (10/10 commits mapped; `stories` attribution empty — subjects name stories in prose, so attribution was done manually by PR number)
  - ✅ `deferred-work.md` — 9 sections of per-story review dispositions (the epic's running finding ledger)
  - ✅ PRs #2–#10 squash-merged to `main`
  - ❌ **No `prd.md` in `_bmad-output/planning-artifacts/`** — FR citations in story files reference `prd.md` line numbers that do not resolve; judged against `epics.md` + `architecture.md`
  - ❌ **No session logs** — process-lesson analysis draws on story Dev Agent Records only
  - ❌ **No prior retro** (Epic 1 `epic-1-retrospective: optional`, never run) — no follow-through section
- **Behavior verification:** real Worker + real D1 (miniflare) exercised via a throwaway smoke run (deleted after): apex bands, `/api/kpis` with `max-age=60`, full poll round-trip (POST → Set-Cookie → voted:true), envelope 404s, opaque admin perimeter. **`/` (the SPA document) could not be verified in the workers pool — the assets layer does not run there**, so production asset serving is verified by nothing anywhere (see Findings V-1).

## Findings

Dispositions: **fix now** (proposed, awaiting go-ahead) / **defer** / **accept** / **decide** (needs Patrick).

### Cross-story defects (invisible to per-story review)

| # | Finding | Source | Disposition |
|---|---------|--------|-------------|
| X-1 | **Three contradictory cert facts render on one page:** poll chrome hard-codes "petition was docketed 28 July 2026" while the seed (`0003_source_fit_corrections.sql:26-33`) says no petition was shown on the docket and the masthead says "the Supreme Court has not been asked yet" | `PollPanel.tsx:33-34`; `Masthead.tsx:38-41`; `migrations/0003:26-33` | **fix now** — reword poll chrome to match the record |
| X-2 | **KPI sub-copy "Of fifty" vs a 51-jurisdiction denominator** (50 states + DC) | `KpiRow.tsx:27`; `publicApi.test.ts` (`statesTotal: 51`); `0002_seed_f1.sql` | **fix now** — interpolate `kpis.statesTotal` |
| X-3 | **`unknown` operational status is the majority state for tracked jurisdictions after 0003 (7 states, 4 platform rows) but the KPI split and filter chips (All/Go/Restricted/Banned) cannot express or select it** | `0003:146-227`; `kpi.ts:16-18`; `KpiRow.tsx:23-38`; `StateFilters.tsx:3-9` | **decide** — chips UI is a product call; the KPI blind spot is a defect either way |
| X-4 | **Page-wide "last updated" freshness excludes `docket_events`** — the table whose `updated_at` 0003 actually bumped — while the masthead presents it as the "as of" for Latest developments | `kpisRepo.ts:55-60`; `Masthead.tsx:72` | **fix now** — add `docket_events` to the freshness union |
| X-5 | **Same user action, two implementations:** issue-map "jump to case" doesn't scroll (`replaceState` only); entity-ledger jump scrolls (`scrollIntoView`) | `IssueBoard.tsx:21-24`; `EntityBoard.tsx:15-23` | **fix now** — align on scroll |
| X-6 | **All five "Report an error" CTAs target `#trust`; 2.10 built `#correct` for exactly this and nav/footer migrated — in-panel CTAs did not, and `states.test.tsx:371` pins the stale anchor** | `StateDetail.tsx:81,219`; `CaseDetail.tsx:185`; `CaseList.tsx:46`; `states.test.tsx:371` | **fix now** — retarget + update pins |
| X-7 | **Reserved `/api/administrivia` returns plain-text 404, breaking the documented `{code,message}` envelope contract for owned `/api/*`** — confirmed live in the behavior check | `publicRouter.ts:39-44`; `server.ts` (plain 404); observed: `404 text/plain "Not found"` | **fix now** — envelope 404 from the router |
| X-8 | **Sunburst silently drops issue tags outside the hard-coded family map** while the same tag renders in the other three views — the vocabulary lives in D1, not in `issueView.ts` | `issueView.ts:63,182` | **fix now** — render an "Other" segment |
| X-9 | **Deep-link params survive as unverifiable once lists settle** (staggered-arrival keep-rule fires after the ready gate; failed list fetch renders as a false "No cases published" claim) — the deferred `useCircuitData` fragility family, re-found at epic view | `selection.ts:29-35`; `useCircuitData.ts:126-131`; deferred from 2.4/2.5/2.6 | **fix now for Epic 3 prep** — distinguish failed from empty; reconcile contradictory `?state`+`?circuit` hydration (`selection.ts:75-96`) |
| X-10 | **Poll integrity copy overstates the mechanism:** two cookie-less tabs (or profiles) mint two tokens; "One vote per browser" is really per-cookie — same accepted class as the 2.9 ballot-stuffing deferral | `publicRouter.ts:163-173`; `usePoll.ts:85-89` | **accept** — within Patrick's 2.9 accepted-risk decision; revisit copy wording only |

### Verification gaps (if it broke, would a test fail?)

| # | Gap | Evidence | Disposition |
|---|-----|----------|-------------|
| V-1 | **Production asset serving verified by nothing:** `/` 404s in the test pool (no assets layer); no post-deploy smoke; README documents `deploy:staging`/`deploy:production` scripts that don't exist | `server.test.ts:373-376`; `docs/deploy-runbook.md`; `README.md` vs `package.json` | **fix now** — deploy-runbook reconciliation + post-deploy smoke checklist |
| V-2 | **`poll_votes` existence in production D1 is unrecorded** — 0005 was only applied locally; until a deploy runs, prod poll endpoints fail and nothing detects it | `2-9 story:32`; runbook (0001–0004 recorded) | **fix now** — record + apply on staging when `build.*` lands |
| V-3 | **No CI anywhere** — tests run only when a human runs them; the Epic 1 lesson (Story 1.3's green claims "neither reproduced") remains structurally unfixed | No `.github/workflows`; `2-1 story:189` | **fix now** — Actions workflow: `npm run check` + `npm test` on PR |
| V-4 | **Interaction logic unexercised:** all 7 fetch hooks + URL selection hook never instantiated in tests; the `inFlight` double-vote fix, `credentials: "same-origin"`, map/ECharts real render paths, popstate restore — all zero-coverage; blocked by the no-new-deps rule that banned a click harness | `usePoll.ts` (all hooks); `2-7 story:119`; deferred-work 2.7 entry | **decide** — allow `@testing-library`/jsdom or add Playwright e2e |
| V-5 | **Silent-skip checks:** bundle secret scans `describe.skipIf(!hasBuild)` no-op on every plain `npm test`; KPI contract tests recompute from sibling endpoints (implementation-pins-implementation); em-dash honesty check is vacuous (`toContain("—")`) | `wranglerConfig.test.tsx:140,160`; `publicApi.test.ts:262-346`; `shells.test.tsx:177-180` | **defer** — tighten opportunistically |
| V-6 | **HEAD/cache/`Secure` unpinned** — dropping HEAD support, flipping cache headers, or dropping the cookie `Secure` flag all pass CI | `publicRouter.ts:82,204`; `respond.ts:15`; `publicApi.test.ts:678-681` | **defer** — cheap pins for a quiet afternoon |

### Aggregate views

- **Architecture delta — clean.** 0 surfaces import `shared/db/repos`; 0 import zod (bundle discipline held across all 10 stories). Selection axes accreted `{state}`→`circuit`→`case`→`issue` through `selection.ts`/`ApexF1Context` without a second selection hook — the per-story "one selection hook" discipline held. `ApexF1Provider` span grew deliberately (2.4→2.7) and 2.8/2.9 correctly refused to extend it.
- **Duplication map — the epic's real structural cost:** 7 hand-rolled `is*` client guards + 7 cloned `AbortController` fetch hooks, each a partial, drifting copy of the server's Zod parse (2.7/2.8 explicitly forked `useOrientation` *to avoid* the deferred `every()` contract). Root cause is a real constraint (zod off the client bundle) that never got a designed answer. `REPO_URL` ×3.
- **God-class growth:** `pml.css` → **2,321 lines** (+1,667 in the epic) — one stylesheet, ten bands; split-by-band is the natural Epic 3 refactor. `publicApi.test.ts` → 743 lines (single coherent suite — acceptable). `casesRepo.ts` → 380 lines with 4-enrichment bulk joins — at the edge; watch it.
- **Pattern divergence:** minor — `renderToStaticMarkup` + mock-pinning held everywhere; a11y friction (`aria-selected`→`aria-pressed` workarounds ×2, one file-level lint disable) is the only recurring divergence from convention.
- **Spec-to-implementation reconciliation:** LaunchNote deletion (2.2), FTS postponement-by-decision (2.5), `#correct` correction shell (2.10), cookie-only identity (2.9) — all recorded deviations. **Unrecorded divergence found:** poll term chrome (X-1) and `unknown` blindness (X-3) contradict shipped data. Epic overview promise ("seed data with provenance and last-updated") met.

### Process lessons (from story records)

1. **The uncommitted-base problem ran the whole epic** — 2.1 done-but-uncommitted under 2.2 (story open question ×3), again 2.9→2.10 (2.10's baseline recorded the raw pre-2.9 SHA). Fixed at the end by the PR-per-story flow; make it standing practice.
2. **The `every()`/parse-all/timeout fragility got worse every time it was deferred** — 2.6's own defer notes its new field *widened* the blast radius; 2.7/2.8 forked hooks to avoid it. Deferring a shared-root bug while patching instances (zero-as-finding class: patched 5× in 5 consecutive stories) is the epic's clearest process lesson.
3. **Patrick's decision backlog grew untracked** — ~10 open questions across stories (donation URL, founder photo/LinkedIn, family column, filter-in-URL, forum filter, entity deep-link, `shortName`, seed cert essay, `?state=` scroll behavior, cert factor count) with no single list to burn down.
4. **oxfmt once-per-story trips** (2.4/2.5/2.6) and a stale change log (2.5) — finalize hygiene without CI.

## Previous-retro follow-through

No Epic 1 retrospective exists (`epic-1-retrospective: optional`, never run). Nothing to check.

## Action items

Proposed — each awaits Patrick's go-ahead before execution:

1. **Stand up `build.predictionmarketlitigation.com` staging** — `env.build` in `wrangler.jsonc` (Worker `pml-build`, own D1 `pml-build`), `deploy:build` script, post-deploy smoke checklist, DNS record (Patrick, dashboard) — *owner: agent + Patrick; gate for Epic 3* — directed by Patrick during this retro
2. **Fix cross-story honesty defect batch** (X-1, X-2, X-4, X-5, X-6, X-7, X-8 + stale `server.ts` "read-only" comment) — *owner: agent; before Epic 3 story 1*
3. **Decide `unknown` status UX** (chips + KPI cell) then fix — *owner: Patrick decides, agent fixes* (X-3)
4. **Break the deferred fragility family** — failed-vs-empty in `useCircuitData`, per-row parse tolerance, fetch timeout, deep-link reconciliation (X-9) — *owner: agent; Epic 3 prep*
5. **Add CI** — GitHub Actions: `npm run check` + `npm test` on every PR (V-3) — *owner: agent*
6. **Decide the click-test harness** (allow jsdom/testing-library vs Playwright e2e) then add coverage for the seven fetch hooks + selection hook (V-4) — *owner: Patrick decides, agent implements*
7. **Burn down the Patrick decision backlog** — one list, one session — *owner: Patrick*
8. **Record remote migration state** — apply 0005 to the staging DB first, then production, with the runbook updated (V-2) — *owner: agent; with action 1*
9. **Restore/locate `prd.md` into planning artifacts** — story FR citations currently dangle — *owner: Patrick/agent; process*
10. **Standing agreement: one merged PR per story before the next story is contexted** — the 2.9→2.10 repeat must not recur

## Acceptance verdict

**accepted-with-open-items** — criteria **declared** (per-story ACs, `epics.md` L384–539). All 10 stories' acceptance criteria verified through per-story acceptance audits, 414 passing tests, and the live behavior check; every story's review conforming with its HALT list; FR44/FR36/FR38/FR39/UX-DR8 spot-checked at epic view. The open items are the findings above — none violates a literal story AC, but X-1/X-2 (self-contradicting public copy on a trust-focused product) and the V-1/V-2 deploy blind spots are real and tracked as actions 1–2 and 8.

## Open questions

- Where is `prd.md`? Story citations point at line numbers of a file planning artifacts doesn't contain.
- Should `build.*` carry production's Access policy on `/admin`, or is the Worker-side backstop enough for a staging surface? (Access application is dashboard-side and invisible to the repo either way.)
- X-3: should `unknown` be selectable, or is the honest fix only the KPI cell?

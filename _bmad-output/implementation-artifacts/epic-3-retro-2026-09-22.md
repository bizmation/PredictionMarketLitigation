---
epic: 3
date: 2026-09-22
verdict: rejected
criteria: declared
headless: false
---

# Retrospective — Epic 3: Governed Daily Loop (Pipeline → Gate → Live)

## Epic summary

- **Epic:** 3 — Governed Daily Loop. `sprint_status.py detect-epic --epic 3`: `story_count: 21`, `pending_stories: []`, `retro_status` was `optional`. Every story key `3-1` through `3-21` is `done`.
- **Diff range:** `36c8abc^..36c91f8` (parent of `story 3.1` through the story 3.20 merge on `main`). `git_evidence.py`: 50 commits, 10 merges, `merges_measured: 10` (no unmeasured merges), 154 paths, non-merge churn **+41,328 / −1,805, net +39,523**, one binary revision. `merge_files` added no path that `files` did not already contain.
- **Story attribution** (non-merge commits whose subject names the id): 3.1×2, 3.2×2, 3.3×2, 3.4×2, 3.5×1, 3.6×1, 3.7×1, 3.8×1, 3.9×1, 3.10×1, 3.11×1, 3.12×1, 3.13×2, 3.14×1, 3.15×1, 3.16×2, 3.17×3, 3.18×3, 3.19×2, 3.20×2, 3.21×1. Two in-range fixes do not name a story id: `82f6974` (disagreement-flag write, PR #19) and `4cd66df` (keep `DailyRunWorkflow` a Workflow binding). Ten other non-merge commits are docs, planning, the Epic 2 staging stand-up, or the landing-page backup.
- **Evidence inventory:**
  - Epic goal and stories 3.1–3.13 acceptance criteria are **declared** in `_bmad-output/planning-artifacts/epics.md` (goal at line 543; story 3.13 ends at line 760).
  - Stories 3.14–3.21 are declared in `epic-3-context.md` and the story specs, and in the 2026-08-09 / 2026-09-17 sprint change proposals. They are **not** in `epics.md`. That gap is already `epic-3-cc-item-1` (open).
  - All 21 `spec-3-*.md` files are `status: done`. `deferred-work.md` holds the per-story deferrals and the 2026-09-17 disposition block.
  - Previous retro: `_bmad-output/implementation-artifacts/epic-2-retro-2026-09-04.md`.
  - **Session logs were not reviewed.** Process notes below come from story specs, `deferred-work.md`, and the deploy runbook. A reader should treat process lessons as narrowed, not as a transcript audit.
- **Going-in concerns:** none supplied with the invocation.

## Findings

Dispositions: **fix now** (proposed, not applied) / **defer** / **accept**.

Code lenses (adversarial, edge-case, verification-gap) were applied **inline on a narrowed scope**: the cross-story seams and the live `pml-build` behavior, not a line-by-line read of the 42k-line diff. Findings without a source were dropped.

### Cross-story defects

| # | Finding | Source | Disposition |
|---|---------|--------|-------------|
| X-1 | **Every CourtListener docket returns HTTP 400, and the Run is `empty`.** On `build.`, `run-20260919-0000`, `run-20260920-0000`, `run-20260921-0000`, and `run-20260922-0000` are `status: "empty"` with `source.fetched` whose four dockets are all `{ error: "http_error", status: 400 }` and `itemCount: 1`. `http_error` is not a source-level failure, so `runConnector` returns `failed: false` and `completeDailyStep` finishes empty. Story 3.4 requires partial connector failure to be explicit on the Run and not marked fully successful (`epics.md` story 3.4). Story 3.21 says a zero-draft day that could not be read is `failed`, never `empty` (`spec-3-21` line 29) for 401/403/429/5xx/network; 400 was implemented as an isolated docket error (`courtListener.ts:243-251`, `512-529`). The live status is the one the matrix did not name, and four days of it look like “nothing changed.” The 400 body is not stored. `run-20260917-0000` and `run-20260918-0000` are the earlier all-`not wired` empties, before this worker revision. | Live `GET /api/runs/:id` on 2026-09-22; `src/pipeline/connectors/courtListener.ts:243-251`, `502-529`; `src/pipeline/connectors/connector.ts:173-222`; `src/pipeline/workflow/dailyRunSteps.ts:145-158`; `spec-3-21-first-live-connector.md:29` | **fix now** — if every polled docket errors, fail the source (`failed: true`) and keep a scrubbed response reason on the evidence |
| X-2 | **Apex still says the pipeline is not live,** on the same worker whose cron has been creating Runs since 2026-09-17. Observed on `https://build.predictionmarketlitigation.com/`: “No pending drafts yet — the daily pipeline is not live”, “ship in Epic 3 and are not live yet”, “once the pipeline ships.” | `src/surfaces/apex/orientation/Masthead.tsx:61-63`; `src/surfaces/apex/ApexShell.tsx:192-200`, `254-259`; browser snapshot of `build.` on 2026-09-22 | **fix now** — retire that copy; the pending-drafts line should follow the live draft list |
| X-3 | **The ops shell cannot be opened on the environment this epic deploys to.** `resolveSurface` returns `ops` only when the hostname starts with `ops.`. `?surface=` is dev-only. `https://build.predictionmarketlitigation.com/runs/run-20260922-0000` renders the apex tracker. There is no staging ops hostname. | `src/shared/lib/surface.ts:29-49`, `63-64`; browser on that URL | **fix now** — a staging hostname (or equivalent) that resolves to the ops shell |
| X-4 | **Public `budgetCents` is null on every staging Run** even though migration 0017 seeded a 500¢ default. Inserts pass `budgetCents: null`; the gateway falls back to the config default only at call time, so the public log does not show the ceiling. | `src/pipeline/workflow/dailyRun.ts:155-165`; `src/pipeline/ai/gateway.ts:219-227`; live run list, all six rows `budgetCents: null` | **fix now** — stamp the ceiling onto the Run at insert |
| X-5 | **`epics.md` still ends at story 3.13.** Stories 3.14–3.21 are the steering channel, hardening, OpenRouter, and the live connector. | `epics.md:760` then Epic 4; `epic-3-context.md:24-31`; `sprint-status.yaml` `epic-3-cc-item-1` | **fix now** — already tracked as `epic-3-cc-item-1`; do not open a second item |
| X-6 | **`epics.md` still says `awaiting-approval` / `budget-stopped`.** Story 3.1 locked `awaiting` / `stopped` / `run.stopped`. `epic-3-context.md` was corrected on 2026-09-17; `epics.md` was not. | `epics.md` story 3.1 (around line 555) and story 3.7 (around line 658); `deferred-work.md` disposition “3.1 epic-context stale identifiers — fixed 2026-09-17” | **fix now** — spec reconciliation of the locked identifiers |
| X-7 | **`run-daily-step` still passes no Workflow step timeout.** The 3.19 deferral asked 3.21 to set one. 3.21 did make docket polls concurrent, so four hung dockets no longer stack 60s deadlines, but a step that exceeds the platform default still bypasses `packageDailyRun`'s catch. | `src/pipeline/workflow/dailyRun.ts:237-243`; `deferred-work.md` 3.19 entry (step timeout); `courtListener.ts:502-507` | **defer** — concurrency removed the stacked-deadline case this story named; set an explicit step timeout when a second live connector lands |
| X-8 | **`withDeadline` still does not cancel `SourceCheck`.** The check type takes no `AbortSignal`. CourtListener times out inside `fetchWithTimeout`; OpenRouter uses `withAbortableDeadline`. A future connector that ignores both keeps running after the deadline. | `src/pipeline/connectors/connector.ts:54-56`, `91-95`; `deferred-work.md` 3.19 `withDeadline` entry; `src/pipeline/ai/gateway.ts:256-263` | **defer** — closed for the two live seams; widen `SourceCheck` with the next connector |
| X-9 | **Story 3.20 is not on `pml-build`.** Worker `modified_on` is `2026-09-18T22:22:32Z` (runbook version `e54cfc19`, main `b1273e6`, story 3.21). `main` HEAD is the 3.20 merge `36c91f8` (2026-09-22). The OpenRouter path `/openrouter/chat/completions` vs `/openrouter/v1/chat/completions` is still unverified. | Cloudflare `workers_list`; `docs/deploy-runbook.md:257`; `deferred-work.md` 3.20 entry; `src/pipeline/ai/gateway.ts` OpenRouter path | **fix now** — deploy current `main` after X-1, then one authenticated gateway call that records which path returned 200 |
| X-10 | **A non-running Run is refused with `GatewayError("budget_stopped")`.** That code is the budget-ceiling reason. This throw does not call `refuseBudget`, so it does not flip the Run to `stopped`, but callers can log the wrong cause. | `src/pipeline/ai/gateway.ts:245-253` | **defer** |

### Aggregate views

- **Architecture delta — clean on the layering rule.** No file under `src/surfaces` imports `pipeline`, `shared/db`, or `zod`. Pipeline modules own D1; surfaces talk to HTTP. Noon ET is the dual UTC cron plus an ET-hour guard (`wrangler.jsonc:126-131`), which is the option story 3.3 allowed.
- **Duplication — the Epic 2 hook copies were not multiplied.** Ops and apex GETs go through `fetchWithTimeout` (`useCircuitData.ts:125`, Evidence detail, pending drafts). Steering and the gate did not grow a second publish path: live F1 writes stay on the Approval Gate (`epic-3-context.md` technical decision; `f1Apply.ts`).
- **Size growth (current line counts, after the epic):** `ApprovalQueue.tsx` 1,026, `EvidenceDetail.tsx` 985, `submitTurn.ts` 879, `draftAndReview.ts` 865, `pml.css` 2,734 (2,321 at the Epic 2 retro). High churn in `git_evidence` `files` is mostly tests (`submitTurn.test.ts` net +2,839, `adminApi.test.ts` +2,557). The four source files are large single-purpose modules. **Defer** a split; they are not a mixed-responsibility class.
- **Pattern divergence — status words held inside the pipeline and drifted in the epic spec and the apex chrome.** Code and `epic-3-context.md` use `awaiting` / `stopped`. `epics.md` and the apex “not live” paragraphs do not (X-2, X-6).
- **Spec-to-implementation reconciliation:** steering stories 3.14–3.18 match `epic-3-context.md` (turns, revision chain, pipeline config, standing guidance cap 12 / 600). Recorded deviations already in `deferred-work.md` (no `gateway.config_changed` event; USD currency hardcoded; prompt-only gateway input) stay **accepted**. Unrecorded divergences are X-1, X-2, X-4, and X-6.

### Verification gaps

| # | Gap | Evidence | Disposition |
|---|-----|----------|-------------|
| V-1 | `DailyRunWorkflow.run` passing `sourceChecksFromEnv` has no executing test. Deleting the argument would make production report CourtListener as “not wired” with the suite green. Live evidence shows the argument is present today (`source.fetched`, not `not wired`). | `deferred-work.md` 3.21 entry; `dailyRun.ts:237-243`; live `run-20260922-0000` | **defer** — the live cron is the current pin; a fake-`step` test still belongs with the next workflow edit |
| V-2 | OpenRouter URL shape is pinned to the spec’s prose and not to a live POST. Same as X-9. | `deferred-work.md` 3.20 entry | **fix now** with the X-9 deploy |
| V-3 | Ops run log, evidence page, and pending-drafts band were not opened on a deployed host. jsdom mount tests exist (`evidenceDetail.mount.test.tsx`, `pendingDrafts.mount.test.tsx`, `approvalQueue.mount.test.tsx`). The deployed check was the JSON API on `build.` plus the apex shell in the browser. | This document’s Behavior verification | **fix now** with X-3, then re-check the three bands |

## Behavior verification

Checked 2026-09-22 against `https://build.predictionmarketlitigation.com` (Worker `pml-build`, last modified 2026-09-18) and the public hosts.

| Check | Observed |
|---|---|
| `GET /api/runs` | 200, six Runs, all `origin: scheduled`, all `status: empty`, `budgetCents: null`, `eventCount: 6`. Ids `run-20260917-0000` through `run-20260922-0000`. |
| `GET /api/runs/run-20260922-0000` (same shape on the 19th, 20th, and 21st) | `source.fetched` CourtListener, four dockets `http_error` 400, three other sources `source.skipped` reason `not wired`, `run.empty` drafts 0, `llmCalls: []`. |
| `GET /api/runs/run-20260917-0000` and `run-20260918-0000` | Four `source.skipped` / `not wired`, then `run.empty`. |
| `GET /api/drafts` | 200 `{"items":[]}`. |
| `GET /api/mode` | `{"mode":"hitl","threshold":70,"version":1,"audit":[]}`. |
| `POST /api/admin/runs` with no operator cookie | 403 `{"code":"forbidden","message":"Operator authorization required."}`. |
| Apex shell in the browser | Tracker settled: cases, docket feed, circuit map, “donations open soon” on both CTAs. Also the “pipeline is not live” copy in X-2. |
| `GET /runs/run-20260922-0000` on `build.` | Apex tracker, not Evidence detail. |
| `https://ops.predictionmarketlitigation.com/` | Landing page (“These will be the features of the website once it gets going”), not the run log. |
| Admin queue, steering, approve/publish | Not exercised. No pending Draft exists, and `/admin` is operator-gated. Unit tests cover those paths; this retro did not click them. |

## Previous-retro follow-through

Epic 2 retro (`epic-2-retro-2026-09-04.md`). Items already `done` in `sprint-status.yaml` were not re-opened. Open items:

| id | Landed? | Evidence | Proposed status |
|---|---|---|---|
| `epic-2-retro-item-3-decide-unknown-operational-status-ux-fil` | No evidence it landed | `StateFilters.tsx:5-7` is still Go / Restricted / Banned. No `unknown` chip. | leave `open` |
| `epic-2-retro-item-4-break-the-deferred-fragility-family-fail` | Partly | Client GET timeout is in `useCircuitData.ts:125` (`fetchWithTimeout`). A failed fetch still marks the slice ready and the board renders empty (`useCircuitData.ts:132-138`). Per-row `every()` drop and deep-link reconciliation were not changed in this reading. | `in-progress` |
| `epic-2-retro-item-6-decide-click-test-harness-jsdom-testing` | Partly | Decision recorded on the item: jsdom + Testing Library. Mount tests exist for Evidence detail, the approval queue, and steering. The seven apex fetch hooks named in the Epic 2 action are not covered by that decision’s “remaining hooks to follow.” | `in-progress` |
| `epic-2-retro-item-7-burn-down-the-patrick-decision-backlog-d` | Partly, the donation anchor only | `DONATE_URL` is still `#coffee` (`ApexShell.tsx:53`). Story 3.19 replaced the dead anchor with visible “donations open soon” (`shells.test.tsx:250-252`). The rest of the backlog (photo, issue family column, filter-in-URL, and the others named on the item) has no landing evidence in this retro. | leave `open` |

These proposed statuses were **not** written. `--set-action-status` waits for confirmation.

## Action items

Proposed — not applied. `epic-3-cc-item-1` (insert stories 3.14–3.21 into `epics.md`) stays as the existing open item; it is not repeated here.

1. **Fail a CourtListener poll when every docket errors, and store a scrubbed response reason.** The Run must be `failed`, not `empty`, and `itemCount` must not read as a successful fetch. Owner: agent. (X-1)
2. **Retire the apex “pipeline is not live” sentences** and drive the pending-drafts line from the draft API. Owner: agent. (X-2)
3. **Make the ops shell reachable on staging**, then open the run log, one evidence page, and the pending-drafts band on that host. Owner: agent + Patrick (hostname). (X-3, V-3)
4. **Stamp the configured budget ceiling onto each new Run** so public `budgetCents` is not null. Owner: agent. (X-4)
5. **Reconcile `epics.md` status words** to `awaiting` / `stopped` / `run.stopped`. Owner: agent. (X-6)
6. **Deploy current `main` to `pml-build` after item 1**, record the next Run id and status, and confirm which OpenRouter gateway path returns 200. Owner: agent + Patrick. (X-9, V-2)

Process lesson, not a code change: a later story that “closes” a failure mode has to name the status the live API returns. 3.21’s matrix listed 401/403/429/5xx/network and the implementation treated every other 4xx as healthy. The live API answered 400.

## Acceptance verdict

**rejected** — criteria **declared** (stories 3.1–3.13 in `epics.md`; stories 3.14–3.21 in their specs and `epic-3-context.md`). `pending_stories` is empty, so this is not an unfinished-story rejection.

Story 3.4’s acceptance criterion is not met on the running staging worker: four consecutive scheduled Runs record a total CourtListener failure and are marked `empty`. The epic’s closing loop (draft → gate → publish) has not happened on a real docket entry: `/api/drafts` is empty, mode is still the HITL default, and no live F1 row has moved through the gate. The pipeline, gate, steering, and public JSON APIs are otherwise present and the operator gate returns 403 anonymously.

A human decision overrides this verdict. Overriding to `accepted-with-open-items` would mean treating X-1 as a tracked defect against an epic whose coded acceptance tests passed, and recording that choice in this section.

## Open questions

- What does CourtListener’s authenticated 400 body say? Evidence stores only `http_error` / `400`. The unauthenticated probe of the same URL returns 401, so the query is at least recognized before auth. The token was not read for this retro.
- Should staging ops be `ops.build.predictionmarketlitigation.com`, a path on `build.`, or wait for the production cutover in `docs/deploy-runbook.md`? X-3 needs that choice before the hostname is created.
- Epic 2 item 3: should `unknown` operational status be a filter chip, or only a KPI cell? Still unanswered.

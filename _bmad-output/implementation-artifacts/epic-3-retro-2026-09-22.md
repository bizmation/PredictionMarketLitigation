---
epic: 3
date: 2026-09-24
original_date: 2026-09-22
verdict: rejected
criteria: declared
headless: false
---

# Retrospective — Epic 3: Governed Daily Loop (Pipeline → Gate → Live)

Resumed 2026-09-24. The latest reassessment is appended below; original findings
remain historical evidence. Current acceptance is **rejected**, based on the reassessment against
5821605 and the deployed staging host below.

## Epic summary

- **Epic:** 3 — Governed Daily Loop. `sprint_status.py detect-epic --epic 3`: `story_count: 21`, `pending_stories: []`, `retro_status` was `optional`. Every story key `3-1` through `3-21` is `done`.
- **Diff range:** `36c8abc^..36c91f8` (parent of `story 3.1` through the story 3.20 merge on `main`). `git_evidence.py`: 50 commits, 10 merges, `merges_measured: 10` (no unmeasured merges), 154 paths, non-merge churn **+41,328 / −1,805, net +39,523**, one binary revision. `merge_files` added no path that `files` did not already contain.
- **Story attribution** (non-merge commits whose subject names the id): 3.1×2, 3.2×2, 3.3×2, 3.4×2, 3.5×1, 3.6×1, 3.7×1, 3.8×1, 3.9×1, 3.10×1, 3.11×1, 3.12×1, 3.13×2, 3.14×1, 3.15×1, 3.16×2, 3.17×3, 3.18×3, 3.19×2, 3.20×2, 3.21×1. Two in-range fixes do not name a story id: `82f6974` (disagreement-flag write, PR #19) and `4cd66df` (keep `DailyRunWorkflow` a Workflow binding). Ten other non-merge commits are docs, planning, the Epic 2 staging stand-up, or the landing-page backup.
- **Evidence inventory:**
  - Epic goal and stories 3.1–3.13 acceptance criteria are **declared** in `_bmad-output/planning-artifacts/epics.md` (goal at line 543; story 3.13 ends at line 760).
  - Stories 3.14–3.21 are declared in `epic-3-context.md` and the story specs, and in the 2026-08-09 / 2026-09-17 sprint change proposals. They are **not** in `epics.md`. That gap is already `epic-3-cc-item-1` (open).
  - All 21 `spec-3-*.md` files are `status: done`. `deferred-work.md` holds the per-story deferrals and the 2026-09-17 disposition block.
  - Previous retro: `_bmad-output/implementation-artifacts/epic-2-retro-2026-09-04.md`.
  - **Session logs were not reviewed.** Process notes below come from story specs, `deferred-work.md`, and the deploy runbook. A reader should treat process lessons as narrowed, not as a transcript audit.
- **Going-in concerns:** none supplied with the invocation.

## Findings

Dispositions: **fix now** (proposed, not applied) / **defer** / **accept**.

Code lenses (adversarial, edge-case, verification-gap) were applied **inline on a narrowed scope**: the cross-story seams and the live `pml-build` behavior, not a line-by-line read of the 42k-line diff. Findings without a source were dropped.

### Cross-story defects

| # | Finding | Source | Disposition |
|---|---------|--------|-------------|
| X-1 | **Every CourtListener docket returns HTTP 400, and the Run is `empty`.** On `build.`, `run-20260919-0000`, `run-20260920-0000`, `run-20260921-0000`, and `run-20260922-0000` are `status: "empty"` with `source.fetched` whose four dockets are all `{ error: "http_error", status: 400 }` and `itemCount: 1`. `http_error` is not a source-level failure, so `runConnector` returns `failed: false` and `completeDailyStep` finishes empty. Story 3.4 requires partial connector failure to be explicit on the Run and not marked fully successful (`epics.md` story 3.4). Story 3.21 says a zero-draft day that could not be read is `failed`, never `empty` (`spec-3-21` line 29) for 401/403/429/5xx/network; 400 was implemented as an isolated docket error (`courtListener.ts:243-251`, `512-529`). The live status is the one the matrix did not name, and four days of it look like “nothing changed.” The 400 body is not stored. `run-20260917-0000` and `run-20260918-0000` are the earlier all-`not wired` empties, before this worker revision. | Live `GET /api/runs/:id` on 2026-09-22; `src/pipeline/connectors/courtListener.ts:243-251`, `502-529`; `src/pipeline/connectors/connector.ts:173-222`; `src/pipeline/workflow/dailyRunSteps.ts:145-158`; `spec-3-21-first-live-connector.md:29` | **fix now** — if every polled docket errors, fail the source (`failed: true`) and keep a scrubbed response reason on the evidence |
| X-2 | **Apex still says the pipeline is not live,** on the same worker whose cron has been creating Runs since 2026-09-17. Observed on `https://build.predictionmarketlitigation.com/`: “No pending drafts yet — the daily pipeline is not live”, “ship in Epic 3 and are not live yet”, “once the pipeline ships.” | `src/surfaces/apex/orientation/Masthead.tsx:61-63`; `src/surfaces/apex/ApexShell.tsx:192-200`, `254-259`; browser snapshot of `build.` on 2026-09-22 | **fix now** — retire that copy; the pending-drafts line should follow the live draft list |
| X-3 | **The ops shell cannot be opened on the environment this epic deploys to.** `resolveSurface` returns `ops` only when the hostname starts with `ops.`. `?surface=` is dev-only. `https://build.predictionmarketlitigation.com/runs/run-20260922-0000` renders the apex tracker. There is no staging ops hostname. | `src/shared/lib/surface.ts:29-49`, `63-64`; browser on that URL | **fix now** — a staging hostname (or equivalent) that resolves to the ops shell |
| X-4 | **Public `budgetCents` is null on every staging Run** even though migration 0017 seeded a 500¢ default. Inserts pass `budgetCents: null`; the gateway falls back to the config default only at call time, so the public log does not show the ceiling. | `src/pipeline/workflow/dailyRun.ts:155-165`; `src/pipeline/ai/gateway.ts:219-227`; live run list, all six rows `budgetCents: null` | **fix now** — stamp the ceiling onto the Run at insert |
| X-5 | **`epics.md` still ends at story 3.13.** Stories 3.14–3.21 are the steering channel, hardening, OpenRouter, and the live connector. | `epics.md:760` then Epic 4; `epic-3-context.md:24-31`; `sprint-status.yaml` `epic-3-cc-item-1` | **fix now** — already tracked as `epic-3-cc-item-1`; do not open a second item |
| X-6 | **`epics.md` still says `awaiting-approval` / `budget-stopped`.** Story 3.1 locked `awaiting` / `stopped` / `run.stopped`. `epic-3-context.md` was corrected on 2026-09-17; `epics.md` was not. | `epics.md` story 3.1 (around line 555) and story 3.7 (around line 658); `deferred-work.md` disposition “3.1 epic-context stale identifiers — fixed 2026-09-17” | **fix now** — spec reconciliation of the locked identifiers |
| X-7 | **`run-daily-step` still passes no Workflow step timeout.** The 3.19 deferral asked 3.21 to set one. 3.21 did make docket polls concurrent, so four hung dockets no longer stack 60s deadlines, but a step that exceeds the platform default still bypasses `packageDailyRun`'s catch. | `src/pipeline/workflow/dailyRun.ts:237-243`; `deferred-work.md` 3.19 entry (step timeout); `courtListener.ts:502-507` | **defer** — concurrency removed the stacked-deadline case this story named; set an explicit step timeout when a second live connector lands |
| X-8 | **`withDeadline` still does not cancel `SourceCheck`.** The check type takes no `AbortSignal`. CourtListener times out inside `fetchWithTimeout`; OpenRouter uses `withAbortableDeadline`. A future connector that ignores both keeps running after the deadline. | `src/pipeline/connectors/connector.ts:54-56`, `91-95`; `deferred-work.md` 3.19 `withDeadline` entry; `src/pipeline/ai/gateway.ts:256-263` | **defer** — closed for the two live seams; widen `SourceCheck` with the next connector |
| X-9 | **Story 3.20 is not on `pml-build`.** Worker `modified_on` is `2026-09-18T22:22:32Z` (runbook version `e54cfc19`, main `b1273e6`, story 3.21). `main` HEAD is the 3.20 merge `36c91f8` (2026-09-22). The OpenRouter path `/openrouter/chat/completions` vs `/openrouter/v1/chat/completions` is still unverified. | Cloudflare `workers_list`; `docs/deploy-runbook.md:257`; `deferred-work.md` 3.20 entry; `src/pipeline/ai/gateway.ts` OpenRouter path | **fix now** — deploy current `main` after X-1, then one authenticated gateway call that records which path returned 200 |
| X-10 | **A non-running Run is refused with `GatewayError("budget_stopped")`.** That code is the budget-ceiling reason. This throw does not call `refuseBudget`, so it does not flip the Run to `stopped`, but callers can log the wrong cause. | `src/pipeline/ai/gateway.ts:245-253` | **defer** |

### Aggregate views

- **Architecture delta — clean on the layering rule.** No file under `src/surfaces` imports `pipeline`, `shared/db`, or `zod`. Pipeline modules own D1; surfaces talk to HTTP. Noon ET is the dual UTC cron plus an ET-hour guard (`wrangler.jsonc:126-131`), which is the option story 3.3 allowed.
- **Duplication — the Epic 2 hook copies were not multiplied.** Ops and apex GETs go through `fetchWithTimeout` (`useCircuitData.ts:125`, Evidence detail, pending drafts). Steering and the gate did not grow a second publish path: live F1 writes stay on the Approval Gate (`epic-3-context.md` technical decision; `f1Apply.ts`).
- **Size growth (current line counts, after the epic):** `ApprovalQueue.tsx` 1,026, `EvidenceDetail.tsx` 985, `submitTurn.ts` 879, `draftAndReview.ts` 865, `pml.css` 2,734 (2,321 at the Epic 2 retro). High churn in `git_evidence` `files` is mostly tests (`submitTurn.test.ts` net +2,839, `adminApi.test.ts` +2,557). The four source files are large single-purpose modules. **Defer** a split; they are not a mixed-responsibility class.
- **Pattern divergence — status words held inside the pipeline and drifted in the epic spec and the apex chrome.** Code and `epic-3-context.md` use `awaiting` / `stopped`. `epics.md` and the apex “not live” paragraphs do not (X-2, X-6).
- **Spec-to-implementation reconciliation:** steering stories 3.14–3.18 match `epic-3-context.md` (turns, revision chain, pipeline config, standing guidance cap 12 / 600). Recorded deviations already in `deferred-work.md` (no `gateway.config_changed` event; USD currency hardcoded; prompt-only gateway input) stay **accepted**. Unrecorded divergences are X-1, X-2, X-4, and X-6.

### Verification gaps

| # | Gap | Evidence | Disposition |
|---|-----|----------|-------------|
| V-1 | `DailyRunWorkflow.run` passing `sourceChecksFromEnv` has no executing test. Deleting the argument would make production report CourtListener as “not wired” with the suite green. Live evidence shows the argument is present today (`source.fetched`, not `not wired`). | `deferred-work.md` 3.21 entry; `dailyRun.ts:237-243`; live `run-20260922-0000` | **defer** — the live cron is the current pin; a fake-`step` test still belongs with the next workflow edit |
| V-2 | OpenRouter URL shape is pinned to the spec’s prose and not to a live POST. Same as X-9. | `deferred-work.md` 3.20 entry | **fix now** with the X-9 deploy |
| V-3 | Ops run log, evidence page, and pending-drafts band were not opened on a deployed host. jsdom mount tests exist (`evidenceDetail.mount.test.tsx`, `pendingDrafts.mount.test.tsx`, `approvalQueue.mount.test.tsx`). The deployed check was the JSON API on `build.` plus the apex shell in the browser. | This document’s Behavior verification | **fix now** with X-3, then re-check the three bands |

## Behavior verification

Checked 2026-09-22 against `https://build.predictionmarketlitigation.com` (Worker `pml-build`, last modified 2026-09-18) and the public hosts.

| Check | Observed |
|---|---|
| `GET /api/runs` | 200, six Runs, all `origin: scheduled`, all `status: empty`, `budgetCents: null`, `eventCount: 6`. Ids `run-20260917-0000` through `run-20260922-0000`. |
| `GET /api/runs/run-20260922-0000` (same shape on the 19th, 20th, and 21st) | `source.fetched` CourtListener, four dockets `http_error` 400, three other sources `source.skipped` reason `not wired`, `run.empty` drafts 0, `llmCalls: []`. |
| `GET /api/runs/run-20260917-0000` and `run-20260918-0000` | Four `source.skipped` / `not wired`, then `run.empty`. |
| `GET /api/drafts` | 200 `{"items":[]}`. |
| `GET /api/mode` | `{"mode":"hitl","threshold":70,"version":1,"audit":[]}`. |
| `POST /api/admin/runs` with no operator cookie | 403 `{"code":"forbidden","message":"Operator authorization required."}`. |
| Apex shell in the browser | Tracker settled: cases, docket feed, circuit map, “donations open soon” on both CTAs. Also the “pipeline is not live” copy in X-2. |
| `GET /runs/run-20260922-0000` on `build.` | Apex tracker, not Evidence detail. |
| `https://ops.predictionmarketlitigation.com/` | Landing page (“These will be the features of the website once it gets going”), not the run log. |
| Admin queue, steering, approve/publish | Not exercised. No pending Draft exists, and `/admin` is operator-gated. Unit tests cover those paths; this retro did not click them. |

## Previous-retro follow-through

Epic 2 retro (`epic-2-retro-2026-09-04.md`). Items already `done` in `sprint-status.yaml` were not re-opened. Open items:

| id | Landed? | Evidence | Proposed status |
|---|---|---|---|
| `epic-2-retro-item-3-decide-unknown-operational-status-ux-fil` | No evidence it landed | `StateFilters.tsx:5-7` is still Go / Restricted / Banned. No `unknown` chip. | leave `open` |
| `epic-2-retro-item-4-break-the-deferred-fragility-family-fail` | Partly | Client GET timeout is in `useCircuitData.ts:125` (`fetchWithTimeout`). A failed fetch still marks the slice ready and the board renders empty (`useCircuitData.ts:132-138`). Per-row `every()` drop and deep-link reconciliation were not changed in this reading. | `in-progress` |
| `epic-2-retro-item-6-decide-click-test-harness-jsdom-testing` | Partly | Decision recorded on the item: jsdom + Testing Library. Mount tests exist for Evidence detail, the approval queue, and steering. The seven apex fetch hooks named in the Epic 2 action are not covered by that decision’s “remaining hooks to follow.” | `in-progress` |
| `epic-2-retro-item-7-burn-down-the-patrick-decision-backlog-d` | Partly, the donation anchor only | `DONATE_URL` is still `#coffee` (`ApexShell.tsx:53`). Story 3.19 replaced the dead anchor with visible “donations open soon” (`shells.test.tsx:250-252`). The rest of the backlog (photo, issue family column, filter-in-URL, and the others named on the item) has no landing evidence in this retro. | leave `open` |

These proposed statuses were **not** written. `--set-action-status` waits for confirmation.

## Action items

Proposed — not applied. `epic-3-cc-item-1` (insert stories 3.14–3.21 into `epics.md`) stays as the existing open item; it is not repeated here.

1. **Fail a CourtListener poll when every docket errors, and store a scrubbed response reason.** The Run must be `failed`, not `empty`, and `itemCount` must not read as a successful fetch. Owner: agent. (X-1)
2. **Retire the apex “pipeline is not live” sentences** and drive the pending-drafts line from the draft API. Owner: agent. (X-2)
3. **Make the ops shell reachable on staging**, then open the run log, one evidence page, and the pending-drafts band on that host. Owner: agent + Patrick (hostname). (X-3, V-3)
4. **Stamp the configured budget ceiling onto each new Run** so public `budgetCents` is not null. Owner: agent. (X-4)
5. **Reconcile `epics.md` status words** to `awaiting` / `stopped` / `run.stopped`. Owner: agent. (X-6)
6. **Deploy current `main` to `pml-build` after item 1**, record the next Run id and status, and confirm which OpenRouter gateway path returns 200. Owner: agent + Patrick. (X-9, V-2)

Process lesson, not a code change: a later story that “closes” a failure mode has to name the status the live API returns. 3.21’s matrix listed 401/403/429/5xx/network and the implementation treated every other 4xx as healthy. The live API answered 400.

## Acceptance verdict

**rejected** — criteria **declared** (stories 3.1–3.13 in `epics.md`; stories 3.14–3.21 in their specs and `epic-3-context.md`). `pending_stories` is empty, so this is not an unfinished-story rejection.

Story 3.4’s acceptance criterion is not met on the running staging worker: four consecutive scheduled Runs record a total CourtListener failure and are marked `empty`. The epic’s closing loop (draft → gate → publish) has not happened on a real docket entry: `/api/drafts` is empty, mode is still the HITL default, and no live F1 row has moved through the gate. The pipeline, gate, steering, and public JSON APIs are otherwise present and the operator gate returns 403 anonymously.

A human decision overrides this verdict. Overriding to `accepted-with-open-items` would mean treating X-1 as a tracked defect against an epic whose coded acceptance tests passed, and recording that choice in this section.

## Open questions

- What does CourtListener’s authenticated 400 body say? Evidence stores only `http_error` / `400`. The unauthenticated probe of the same URL returns 401, so the query is at least recognized before auth. The token was not read for this retro.
- Should staging ops be `ops.build.predictionmarketlitigation.com`, a path on `build.`, or wait for the production cutover in `docs/deploy-runbook.md`? X-3 needs that choice before the hostname is created.
- Epic 2 item 3: should `unknown` operational status be a filter chip, or only a KPI cell? Still unanswered.

## Completion reconciliation — 2026-09-24

At Patrick's request, Epic 3 is marked done in sprint tracking under its defined
meaning: all 25 story implementations are complete. Story 3.12 was already done;
stale review statuses for 3.22 and 3.23 are now done. This does not replace the
historical rejected acceptance verdict with an unobserved success.

Stories 3.23, 3.24, 3.25 and 3.22 address the all-docket failure classification,
stale pipeline copy, Run budget snapshot and staging ops host respectively.
The 2026-09-24 staging deployment and browser checks are recorded in
`docs/deploy-runbook.md`. Status-word reconciliation is already present in
`epics.md`; its matching action is closed. The retired-copy action remains
partially open because the apex pending-draft count is still not API-driven.

Remaining closure work:
- Backfill stories 3.14–3.21 into epics.md (existing correct-course item).
- Wire the apex pending-draft count to live draft data.
- Inspect a post-deployment Run, including connector failure diagnostics and
  the new budget snapshot; confirm a successful authenticated OpenRouter
  gateway call and its URL shape.
- Demonstrate a real docket entry through draft → gate → publish, then update
  operational acceptance from observed evidence. No new Run or publish was
  performed as part of this tracking reconciliation.

Earlier explicitly deferred technical items remain deferred; marking story
implementation complete does not silently resolve them. Story 3.22 is committed
locally as 5821605 and deployed to staging; its push/PR/merge remains separate.

## 2026-09-24 reassessment — working record

### Epic summary and inventory

Explicit user request: retrospective on Epic 3. Script-backed completeness:
25 story keys, pending_stories [], all 25 spec frontmatters done. Sprint status
marks Epic 3 and the retrospective done; neither overrides the acceptance verdict.
Range: 36c8abc^..5821605, including the first story's parent through story 3.22.
Git evidence: 60 commits, 14 merges, 13 measured on the first-parent spine;
one merge is not independently measured. No merge churn is added to non-merge
churn. Story-number-free commits are mapped through their changed specs and PRs,
not falsely treated as absent implementations.

Available: original epic criteria, story specs 3.1–3.25, change proposals,
architecture/PRD, full staged epic diff, Git history, sprint actions, previous
Epic 2 retro, original Epic 3 retro, current-turn deployment/browser observations
and fresh read-only public APIs. Full historical session logs were not loaded;
process observations are limited to documented artifacts and this conversation.
Current uncommitted status reconciliation is retained separately from the code
range. This is a resumed reassessment, not a rewrite of the 2026-09-22 history.

### Findings and aggregate views

BMAD Review ran adversarial, edge-case-hunter and verification-gap lenses on
`36c8abc^..5821605` (full diff staged once). Three independent reviewers returned
12 adversarial observations, 12 edge cases and one verification gap. The parent
reopened each cited source and its guards/callers. Findings below consolidate
overlap; no production race or paid call was induced. Concurrency findings are
source-validated interleavings, not claims of observed production incidents.

| ID / lenses | Verified finding and source | Instance disposition | Prevention |
|---|---|---|---|
| R1 / edge | A root Draft with null evalSummary is considered ready (`draftsRepo.ts:73`); `approval.ts:143–155` does not exclude running Runs. Server decision route adds no readiness guard (`server.ts:202–270`). Evaluation selects null-eval Drafts without excluding decided rows (`draftAndReview.ts:673`) and review UPDATE is unconditional by ID (`draftsRepo.ts:340`). Approval can publish before evaluation, then the evaluator changes the sealed Draft. | fix now | Integration test pausing evaluation, attempting approval, and verifying approved text cannot mutate. |
| R2 / adversarial + edge | Final sibling decisions read `otherPending` before their batches (`approval.ts:248–273`). Two snapshots can each see the other sibling pending, so both decisions commit without terminalizing the Run. | fix now | Decide terminal state from transactional current state; overlap two decisions in a test. |
| R3 / adversarial + edge | Parent readiness/no-successor is checked before batch (`approval.ts:150–155`); publication SQL guards only pending outcome (`f1Apply.ts:160`, `323`). A child revision inserted between checks and batch allows a superseded parent to publish. | fix now | Serialize or atomically guard revision and approval per chain. |
| R4 / adversarial + edge | Docket state-patch from-values are read/validated before batch (`f1Apply.ts:234–245`) and absent from UPDATE predicates (`320–325`); generic updates ignore from-values entirely (`375–402`). Stale or concurrent proposals can overwrite a newer state. These are two variants of missing transactional state comparison. | fix now | Compare-and-set every accepted field and abort the complete publication batch on conflict. |
| R5 / adversarial + edge | Budget uses ledger read followed by provider call without reservation (`gateway.ts:226–261`). Concurrent requests can both pass the same remaining-budget check. | fix now | Atomic reservation and deterministic parallel-call test. |
| R6 / adversarial + edge | OpenRouter estimate covers input only and request has no output bound (`gateway.ts:425–439`), while recorded cost includes output (`462–469`). An admitted call can exceed the configured estimate/ceiling. Actual dollar charging is not validated by the heuristic either. | fix now | Bound/reserve output using an explicit cost policy; distinguish measured provider cost from estimates. |
| R7 / adversarial + edge | LLM ledger insert and Run spend bump are separate awaits (`gateway.ts:281–294`). Failure between them leaves displayed spend below the enforcement ledger. | fix now | Atomic accounting batch or derive display from authoritative ledger. |
| R8 / edge | On packaging replay after Draft insertion but before Workflow checkpoint, `seenEventIds` includes those Drafts (`courtListener.ts:233–251`); packaging counts only newly returned Drafts (`dailyRunSteps.ts:176–182`, `234–242`). Zero on replay can mark a material Run empty and skip review. | fix now | Resume from persisted Run Drafts and test replay at the persistence/checkpoint boundary. |
| R9 / adversarial | `run.started` captures pollSourcesVersion in attach (`dailyRunSteps.ts:83–95`), but packaging rereads effective sources (`176`). A config change between steps makes the receipt version differ from the sources actually consumed. | fix now | Pin the config or record its actual consumption version. |
| R10 / adversarial | `kickDailyRun` catches every create error as a duplicate (`dailyRun.ts:67–74`). An outage or permission failure is silently lost with no Run record. | fix now | Classify duplicate errors narrowly and surface other dispatch failures. |
| R11 / edge | Manual and catch-up admission reads date state before inserting origin-specific IDs (`dailyRun.ts:123–168`); no active-date uniqueness constraint exists in migrations. Concurrent differing origins can both pass and consume budget. | fix now | Atomic date admission across operator and scheduled paths. |
| R12 / edge | `fetchPage` attaches the token to its URL (`courtListener.ts:282–297`); pagination accepts next from response and follows it without an origin check (`359–363`). A foreign next URL would receive that credential. No such live response was observed. | fix now | Restrict pagination destinations to the intended HTTPS API origin before sending authorization. |
| R13 / adversarial + edge | Poll floor advances to latest published event (`courtListener.ts:430–443`, `457–460`). Late RECAP entries older than that floor are skipped even if unseen. The code deliberately avoids historical backfill; the unresolved product choice is how to accommodate delayed arrivals. | defer | Specify an overlap/backfill policy and test out-of-order arrival, without silently broadening historical scope. |
| R14 / adversarial | Failed child revision remains non-ready and blocks its chain (`submitTurn.ts:265–272`; prior spec-3-16 review records this decision). It is fail-closed but has no recovery transition. | defer | Explicit operator retry/abandon policy preserving attempted-revision evidence. |
| R15 / verification-gap | No test invokes DailyRunWorkflow.run (`dailyRun.ts:221–255`). Tests manually call packageDailyRun then reviewDailyRun (`dailyRun.test.ts:801–837`); test Wrangler omits workflow binding (`wrangler.test.jsonc:56`). Removing registry forwarding or draft-and-review dispatch can leave helper tests green. | fix now | Executing orchestration test with real helpers and deterministic fake external providers. |
| R16 / direct reconciliation | Apex Masthead always says no pending drafts (`Masthead.tsx:62`); ApexShell supplies no live draft count (`ApexShell.tsx:106`). Old action 13 is only partly closed by 3.24. | fix now, existing item | Make copy derive from the same public draft data readers inspect. |
| R17 / direct reconciliation | Stories 3.14–3.21 remain outside epics.md (`epics.md:763`). Original status-word defect is corrected at stories 3.1/3.7. | fix now, existing backfill item | Update canonical epic spec when expanding stories. |
| R18 / behavior | No post-deploy Run/gateway/draft/publish evidence exists in the current public records. Today's latest Run predates the deployment and draft list is empty. | fix now, existing deploy-verification item plus explicit end-to-end acceptance item | Separate implementation, deploy, and observed acceptance gates. |

Rejected review observation: staging apex links still point to production
(`surface.ts:112–116`). This is accurate behavior but not a violated 3.22 contract:
its host-aware change explicitly covers ops links and preserves apex origins.
Retain as an optional navigation product decision, not a blocking regression.

Aggregate measurements: 165 paths; non-merge additions 42,909, deletions 1,938,
net +40,971; one binary revision unmeasured. Merge evidence is separate, with
13 of 14 merges measured. Current large modules: ApprovalQueue 1,026 lines,
EvidenceDetail 993, submitTurn 879, draftAndReview 865, SteeringPanel 610,
CourtListener 582, draftsRepo 477, gateway 473. These sizes identify review seams;
size alone is not a defect or a demand to split them.

Architecture spot-check: no production surface imports pipeline, shared/db or
zod; source import search agrees with architecture.md:520–522. No full module
cycle graph was constructed, so this is a boundary check, not proof of absence
of cycles. Duplication review is narrowed to gate, workflow and gateway: the
shared gate remains the publication path, but invariant checks are repeated as
preflight reads instead of atomic transaction guards. Pattern divergence is
primarily read-then-write protection versus the atomic guarantees the stories
claim. No new broad duplication audit or historical-session process audit was
performed. Subagent slots were used for the three review lenses; aggregate
measurements and reconciliation were performed by the parent.

Prior fixes confirmed: X-1 is implemented by 3.23; X-2 stale copy by 3.24 (count
still open); X-3/V-3 by deployed 3.22; X-4 by 3.25 for new Runs; X-6 identifiers
are reconciled; X-9 deployment portion is complete. Historical records are not
rewritten to pretend they ran the new code. Deferred step timeout, SourceCheck
cancellation, and non-running budget_stopped error labeling remain as recorded
in the original retro and deferred ledger.

### Behavior verification

Fresh public APIs on ops-build returned eight scheduled Runs through
run-20260924-0000; all predate the staging update, are empty, have six events and
legacy null budgets. Draft list is empty; mode is HITL with threshold 70 and no
audit changes. These records cannot establish post-fix failure classification,
a new budget snapshot, or a successful new gateway/draft/publish cycle.

Additional browser check in this reassessment: opened the existing Evidence
page, followed Run log back to ops-build, observed eight loaded rows and the
empty pending-drafts band, and checked all row hrefs remain on ops-build.
Earlier same-session deploy evidence remains in docs/deploy-runbook.md: Worker
98ed45b4-f131-4d40-b5b7-944138869779, no migrations pending, 1,050 tests across
48 files passing, production domain ownership and response hashes unchanged,
admin API anonymous 403. Tests were not rerun just to write this retrospective;
no runtime source changed after those gates. Authenticated approval, paid
provider calls and live publishing were not exercised in this retrospective.

### Previous-retro follow-through (Epic 2)

Already-done items were not reopened. Remaining selectors and primary evidence:

| Exact item ID | Evidence and proposed status |
|---|---|
| epic-2-retro-item-3-decide-unknown-operational-status-ux-fil | StateFilters.tsx:3–9 still has All/Go/Restricted/Banned only; no evidence of a completed UX decision. Keep open. |
| epic-2-retro-item-4-break-the-deferred-fragility-family-fail | useCircuitData.ts:125 uses fetchWithTimeout, but catch at 132–135 still makes failure a ready empty slice. Partial; proposed in-progress. |
| epic-2-retro-item-6-decide-click-test-harness-jsdom-testing | Harness decision is recorded in the sprint action; Evidence/admin mount tests exist, but complete seven-hook + selection coverage is not established. Partial; proposed in-progress. |
| epic-2-retro-item-7-burn-down-the-patrick-decision-backlog-d | Story 3.19 documents donation placeholder repair; no evidence found of closing the full enumerated decisions. Keep open. |

No earlier-epic status transitions were applied by this retrospective. The
proposals above remain available for a separate tracking decision.

### Action items

Remediation is proposed, not implemented by the retrospective. Grouped work:

1. **Gate transaction safety** (R1–R4): prevent early approval and post-decision
   mutation; make readiness, revision tip, stale-field checks and Run finalization
   atomic; add overlapping-decision/revision tests. Owner: agent.
2. **Paid-budget/accounting safety** (R5–R7): reserve concurrent spend, bound
   output using explicit cost semantics and commit ledger/Run accounting
   together. Owner: agent; Patrick confirms cost policy if required.
3. **Workflow replay and admission safety** (R8–R11): recover persisted Drafts,
   consume recorded config, preserve dispatch failures and enforce cross-origin
   Run admission. Owner: agent.
4. **Connector credential boundary** (R12): validate pagination origin before
   authenticated fetch. Owner: agent.
5. **Executing Workflow regression test** (R15): run the actual entrypoint
   through material and failure paths, preserving connector forwarding and
   evaluated awaiting Drafts. Owner: agent.
6. **Live governed-loop acceptance** (R18): record the post-deploy Run and
   authenticated gateway outcome, then demonstrate a real docket Draft through
   named approval to published F1 with matching public evidence. Owner: agent +
   Patrick. Existing item 17 retains the first portion; the full acceptance
   demonstration is a distinct explicitly tracked item.

Existing items are reused, not duplicated: item 13 live pending-draft count;
correct-course item 1 epic-spec backfill; item 17 post-deploy Run/gateway check.
R13 delayed-arrival policy and R14 failed-revision recovery remain deferred,
with source and decision needed recorded above. No application fix, paid call,
Run trigger, approval, or publication was performed by this retrospective.

Process recommendations: include one interruption/replay scenario and one
concurrent caller scenario in cross-story acceptance for stateful operations;
label implementation done, deployed, and behavior accepted separately; require
public receipts to name configuration actually consumed, not merely observed
at Run creation. These derive from cited code/artifacts, not inferred motives
of earlier sessions.

### Acceptance verdict

**rejected** — criteria **declared**. All 25 stories are marked done and no
unfinished-story gate applies, but done is not operational acceptance. R1–R12
and R15 expose unclosed governance invariants across story boundaries; R18
leaves the real governed loop undemonstrated. Staging reachability, updated
copy and budget display are verified improvements over the original retro.
They do not settle publication atomicity, paid-budget enforcement or replay
correctness. No human override of this evidence-based verdict was requested.

### Open questions

- What output/cost policy should define a strict paid Run ceiling (versus the
  current token heuristic)? Resolve while implementing budget reservation.
- What delayed-RECAP overlap and failed-revision recovery behavior should be
  supported? Both need explicit product rules before implementation.
- The live authenticated CourtListener failure reason and gateway success path
  remain unobserved after the new deployment; a controlled operational check
  is still needed.

Review accounting: adversarial 12 + edge 12 + verification 1 = 25 reports;
24 retained reports consolidate into R1–R15 (R4 includes generic and docket
variants), one staging-apex observation rejected on explicit scope. R16–R18
are direct reconciliation/behavior findings. Full finding fields are captured
by the table's source, trigger, consequence, disposition and prevention columns.


### 2026-09-25 corrective closure — gate transaction safety

R1–R4 implementation remediation is complete through [3.28 / PR #44](https://github.com/bizmation/PredictionMarketLitigation/pull/44) and [3.29 / PR #45](https://github.com/bizmation/PredictionMarketLitigation/pull/45). Story 3.28 enforces persisted readiness and sealed evaluator writes; 3.29 commits current-head/snapshot validation, canonical prior-value comparison, decision ownership, revision admission and Run finalization atomically. Deterministic real-D1 tests cover both decision/revision orderings, duplicate and sibling decisions, stale generic/docket publication, whole-batch rollback and stopped human-review behavior; HTTP/public Evidence and mounted UI tests cover their exposed contracts.

Implementation commit `0d6c39b8c785405d8575255cd208f55a8fba0bd3` passed [required CI](https://github.com/bizmation/PredictionMarketLitigation/actions/runs/36147854656): 1148 passed, 6 environment-dependent skipped, 50 files; all 1154 passed locally. The linked sprint gate-safety action is done. Large-Run query performance remains an explicitly unverified follow-up in the 3.29 spec.

The Epic 3 acceptance verdict remains **rejected** until later corrective work, staging verification and the 3.37 reassessment. Migration 0019 is included but not remotely applied; this closure records reviewed implementation evidence, not deployment or live governed-loop acceptance.
